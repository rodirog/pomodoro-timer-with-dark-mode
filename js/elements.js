const minutesDisplay = document.querySelector(".minutes")
const secondsDisplay = document.querySelector(".seconds")
const buttonPlay = document.querySelector(".play")
const buttonPause = document.querySelector(".pause")
const buttonStop = document.querySelector(".stop")
const buttonIncrease = document.querySelector(".increase-minutes")
const buttonDecrease = document.querySelector(".decrease-minutes")
const soundOnForest = document.querySelector(".card.forest-on")
const soundOnRain = document.querySelector(".card.rain-on")
const soundOnCoffeeShop = document.querySelector(".card.coffee-shop-on")
const soundOnFireplace = document.querySelector(".card.fireplace-on")
const soundOffForest = document.querySelector(".card.forest-off")
const soundOffRain = document.querySelector(".card.rain-off")
const soundOffCoffeeShop = document.querySelector(".card.coffee-shop-off")
const soundOffFireplace = document.querySelector(".card.fireplace-off")
const buttonLightMode = document.querySelector(".light-mode-sun")
const buttonDarkMode = document.querySelector(".dark-mode-moon")
const timerDisplay = document.querySelector(".timer")
const body = document.querySelector("body")
const controlButtons = document.querySelector(".controls")
const volumeControlForestOn = document.querySelector(".volume-control.forest-on")
const volumeControlRainOn = document.querySelector(".volume-control.rain-on")
const volumeControlCoffeeShopOn = document.querySelector(".volume-control.coffee-shop-on")
const volumeControlFireplaceOn = document.querySelector(".volume-control.fireplace-on")
const volumeControlForestOff = document.querySelector(".volume-control.forest-off")
const volumeControlRainOff = document.querySelector(".volume-control.rain-off")
const volumeControlCoffeeShopOff = document.querySelector(".volume-control.coffee-shop-off")
const volumeControlFireplaceOff = document.querySelector(".volume-control.fireplace-off")

export { 
    minutesDisplay,
    secondsDisplay,
    buttonPlay,
    buttonPause,
    buttonStop,
    buttonIncrease,
    buttonDecrease,
    soundOnForest,
    soundOnRain,
    soundOnCoffeeShop,
    soundOnFireplace,
    soundOffForest,
    soundOffRain,
    soundOffCoffeeShop,
    soundOffFireplace,
    buttonLightMode,
    buttonDarkMode,
    timerDisplay,
    body,
    controlButtons,
    volumeControlForestOn,
    volumeControlRainOn,
    volumeControlCoffeeShopOn,
    volumeControlFireplaceOn,
    volumeControlForestOff,
    volumeControlRainOff,
    volumeControlCoffeeShopOff,
    volumeControlFireplaceOff
  }
